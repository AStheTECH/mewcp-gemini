# Gemini API Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Deployment Mode
# Set to "hosted" for production behind a gateway
# Leave as "oss" (or unset) for local/self-hosted
FASTMCP_CREDENTIAL_MODE=oss
